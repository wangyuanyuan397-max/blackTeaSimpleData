name: ortho_a06_mixup_a0p4
orthoshot_variant: A06
random_seed: 2026
model:
  type: classifier
  strategy: classification
  backbone: {type: timm, model_name: mixnet_s, pretrained: true, input_size: 408}
  head: {type: linear, drop_rate: 0.0}
train:
  orthoshot:
    enabled: true
    augmentation: {mode: mixup, probability: 0.5, mixup_alpha: 0.4}
