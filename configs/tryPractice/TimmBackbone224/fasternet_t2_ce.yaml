name: fasternet_t2_ce
model:
  type: classifier
  strategy: classification
  backbone:
    type: timm
    model_name: fasternet_t2.in1k
    pretrained: true
  head:
    type: linear
    drop_rate: 0.0
