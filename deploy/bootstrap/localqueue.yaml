apiVersion: kueue.x-k8s.io/v1beta2
kind: LocalQueue
metadata:
  name: shared
  namespace: p-wyy-black-tea-dev
  labels:
    app.kubernetes.io/part-of: black-tea-simple
    app.kubernetes.io/environment: dev
spec:
  clusterQueue: lab-shared

