apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: p-wyy-black-tea-dev

resources:
  - namespace.yaml
  - localqueue.yaml
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/environment: dev

